"use client"
import { useState } from "react"
import { useRouter } from "next/navigation"
import { loginUser } from "@/lib/storage"
export default function LoginPage(){const router=useRouter(); const [email,setEmail]=useState(""); const [password,setPassword]=useState(""); const [message,setMessage]=useState(""); async function handleSubmit(e){e.preventDefault(); setMessage(""); try{await loginUser(email,password); router.push("/profile")}catch(error){setMessage(error.message||"Přihlášení se nepovedlo.")}} return <main className="page-shell"><div className="glass card center-wrap"><span className="badge">Přihlášení</span><h1 className="title-lg" style={{marginTop:14}}>Přihlášení do ligy</h1><form className="stack" onSubmit={handleSubmit}><div><label className="label">E-mail</label><input className="input" type="email" value={email} onChange={(e)=>setEmail(e.target.value)} /></div><div><label className="label">Heslo</label><input className="input" type="password" value={password} onChange={(e)=>setPassword(e.target.value)} /></div><div className="row"><button className="button button-primary" type="submit">Přihlásit</button>{message?<div className="muted">{message}</div>:null}</div></form></div></main>}
