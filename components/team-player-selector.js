"use client"
export default function TeamPlayerSelector({title,teams,selectedTeam,onSelectTeam,players,selectedPlayers,onTogglePlayer,limit,blockedIds}){return <div className="player-select-card"><h2 className="title-md">{title}</h2><div style={{marginBottom:14}}><label className="label">Vyber tým</label><select className="select" value={selectedTeam} onChange={(e)=>onSelectTeam(e.target.value)}><option value="">Vyber tým</option>{teams.map((team)=><option key={team} value={team}>{team}</option>)}</select></div><div className="stack">{!selectedTeam?<div className="empty">Nejdřív vyber tým.</div>:null}{players.map((player)=>{const selected=selectedPlayers.some((p)=>p.id===player.id); const blocked=blockedIds.includes(player.id); return <button key={player.id} type="button" className="player-button" disabled={blocked && !selected} onClick={()=>onTogglePlayer(player)}><strong>{player.firstName} {player.lastName}</strong><br /><span className="muted">{player.team} • {player.gender==="female"?"Žena":"Muž"} {selected?"• vybrán":""}</span></button>})}{selectedTeam && !players.length?<div className="empty">V tomhle týmu nejsou v této lize žádní hráči.</div>:null}</div><div className="notice" style={{marginTop:14}}>Vybráno: {selectedPlayers.length} / {limit}</div></div>}
