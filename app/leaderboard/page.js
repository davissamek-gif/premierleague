"use client"
import { useEffect, useMemo, useState } from "react"
import { buildTeamStats, getAccuracy, getExistingTeams, getPlayers } from "@/lib/storage"
export default function LeaderboardPage(){const [players,setPlayers]=useState([]); const [teams,setTeams]=useState([]); const [leagueFilter,setLeagueFilter]=useState("all"); const [genderFilter,setGenderFilter]=useState("all"); const [teamFilter,setTeamFilter]=useState("all"); useEffect(()=>{getPlayers().then(setPlayers); getExistingTeams().then(setTeams)},[]); const teamStats=useMemo(()=>buildTeamStats(players),[players]); const rows=useMemo(()=>players.filter((p)=>leagueFilter==="all"?true:p.league===leagueFilter).filter((p)=>genderFilter==="all"?true:p.gender===genderFilter).filter((p)=>teamFilter==="all"?true:p.team===teamFilter).map((p)=>({...p,accuracy:getAccuracy(p.hits,p.misses),totalWins:(p.wins||0)+(p.overtimeWins||0),teamTotalWins:teamStats[p.team]?.totalWins||0,teamAccuracy:teamStats[p.team]?.accuracy||0})).sort((a,b)=>b.totalWins-a.totalWins || b.accuracy-a.accuracy || (b.hits||0)-(a.hits||0)),[players,leagueFilter,genderFilter,teamFilter,teamStats]); const top3=rows.slice(0,3); return <main className="page-shell"><div className="glass card"><div className="row" style={{justifyContent:"space-between",marginBottom:16}}><div><span className="badge">Žebříček</span><h1 className="title-lg" style={{marginTop:14,marginBottom:0}}>Žebříček hráčů</h1></div><div className="filters-row"><select className="select" value={leagueFilter} onChange={(e)=>setLeagueFilter(e.target.value)}><option value="all">Všechny ligy</option><option value="first">1. liga</option><option value="second">2. liga</option></select><select className="select" value={genderFilter} onChange={(e)=>setGenderFilter(e.target.value)}><option value="all">Muži + Ženy</option><option value="male">Muži</option><option value="female">Ženy</option></select><select className="select" value={teamFilter} onChange={(e)=>setTeamFilter(e.target.value)}><option value="all">Všechny týmy</option>{teams.map((team)=><option key={team} value={team}>{team}</option>)}</select></div></div>{!!top3.length?<div className="top3">{top3[0]?<div className="top-card gold"><div className="muted">1. místo</div><div className="top-number">{top3[0].firstName} {top3[0].lastName}</div><div className="muted">{top3[0].team} • {top3[0].totalWins} výher • {top3[0].accuracy}%</div></div>:null}{top3[1]?<div className="top-card silver"><div className="muted">2. místo</div><div className="top-number">{top3[1].firstName} {top3[1].lastName}</div><div className="muted">{top3[1].team} • {top3[1].totalWins} výher • {top3[1].accuracy}%</div></div>:null}{top3[2]?<div className="top-card bronze"><div className="muted">3. místo</div><div className="top-number">{top3[2].firstName} {top3[2].lastName}</div><div className="muted">{top3[2].team} • {top3[2].totalWins} výher • {top3[2].accuracy}%</div></div>:null}</div>:null}<div className="notice" style={{marginBottom:14}}>Hráčský leaderboard drží přehled o formě hráče i síle jeho týmu. Samostatný týmový board přijde později.</div><div className="table-wrap"><table className="table"><thead><tr><th>#</th><th>Hráč</th><th>Tým</th><th>Liga</th><th>Pohlaví</th><th>W</th><th>OW</th><th>OL</th><th>L</th><th>Hit</th><th>Miss</th><th>% hráče</th><th>Výhry týmu</th><th>% týmu</th></tr></thead><tbody>{rows.map((player,index)=><tr key={player.id}><td><span className={`rank ${index===0?"top":""}`}>{index+1}</span></td><td><strong>{player.firstName} {player.lastName}</strong></td><td>{player.team}</td><td>{player.league==="first"?"1. liga":"2. liga"}</td><td>{player.gender==="female"?"Žena":"Muž"}</td><td>{player.wins||0}</td><td>{player.overtimeWins||0}</td><td>{player.overtimeLosses||0}</td><td>{player.losses||0}</td><td>{player.hits||0}</td><td>{player.misses||0}</td><td>{player.accuracy}%</td><td>{player.teamTotalWins}</td><td>{player.teamAccuracy}%</td></tr>)}</tbody></table></div></div></main>}
